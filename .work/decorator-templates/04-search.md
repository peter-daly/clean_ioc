# M04 search gate

Accepted read-only evidence from `/root/m04_search`, `gpt-6-luna`, low. Baseline `530a725`.

- `_Layer`/`_Blueprint` (`container.py:499/547`) retain immutable composition snapshots. `_BuilderBase._layer` materializes discovery before freezing. Enumerate exact visible sources with `registrations(for_each, declaration_area)`, deduplicating original IDs and preserving original definition service/ownership.
- `_Compiler._compile_source_core` (~3580) is the accepted disposable frozen undecorated graph seam. Supply identical build arguments/anchor/preconfiguration/owner/inherited explanation inputs; never reuse its compiler for runtime. `_compile_decorators` suppresses all decorator branches in inspection mode.
- Source metadata types do not yet exist. Preserve raw static implementation aliases separately from normalized Component.implementation_type. `_project_service_type(implementation, base)` supplies TypeVar-identity inherited bindings, including generated subclasses; broad factory types stay unknown and are never activated.
- Add internal immutable declaration/specification/source-info/expansion records and layer retention. Shared ordinal with ordinary decorators, UUID5(template ID, source ID), source ordinal and area provenance. Keep generated results outside builder state and original declarations separate for retries/overlays.
- `_compile_with_report` insertion seam: alias normalization -> ordinary boundary visibility -> expansion -> one consistency recheck -> fresh runtime compiler. M04 exposes internal expansion output only; M05 wires complete generated target compilation and public entry points. No silently ineffective public template registration API.
- Recheck primitive should accept an expanded normalized blueprint and original prepared visibility, call `_prepare_boundary_visibility` once and compare ordered uses/exposes; changed or newly invalid means template-visibility-cycle with original cause. M04 can exercise primitive with synthetic ordinary generated-equivalent decorators. Such tests do not certify actual DerivedServices/group generated decorator semantics: M05 must wire and test recheck with its complete target compiler before public API exposure. No callback fixed-point loop.
- `_assert_mutable` currently only checks built state. Expansion needs scoped mutation/build/preview reentry guard with finally cleanup; callback failure must leave retryable original state. Keep expansion outside diagnostic root retries to avoid inadvertent callback replay.
- Existing executable references:14 M01 feasibility probes, M03 service-target tests, boundaries, patterns, closed constructors, resource ownership, compiler tooling, groups and container suites.

Coordinator reviewed builder build/preview and source/visibility seams. Implementation uses full accepted01-handoff staging and03-handoff projection contract, not search suggestions where they conflict. No search source edits/tests; no bark-core access needed.
