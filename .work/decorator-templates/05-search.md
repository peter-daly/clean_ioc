# M05 search gate

Accepted read-only evidence `/root/m05_search`, `gpt-6-luna`, low; baseline `596e12a`.

- `_compile_with_report` (`container.py:6978`) normalizes aliases/prepares visibility before fresh compiler and handles diagnostic retries. Insert expansion once outside root retries, retain initial visibility, attach immutable candidates/evidence separately from originals, normalize expanded snapshot, run actual generated boundary compilation and M04 one-shot check, then fresh runtime compilation. Preserve BuildReport/PartialGraph handling for phase errors.
- Preview (`_preview_components:8307`) currently bypasses `_compile_with_report`; wire equivalent full semantics. Root/scope build share compile helper; active-owner mutation/build/preview guards already exist.
- M04 `_decorator_templates.py` records and `_expand_decorator_templates:6821` preserve source IDs,areas,owners,source ordinals and shared decorator order. `_template_sources:6803` orders declarations, not newest-first lookup. Never store generated candidates as declarations.
- `_select_service_target(s):4095/4127` supplies original target ID/request/projected contract. Bind decorated-argument annotation using identity-keyed `_bind_typevar_identities` and substitute `_resolve_typevar_identities`; do not pass distinct same-name vars through legacy name-keyed inference. Exact source argument policy compiles normally, enforcing contextual conditions/visibility without fallback.
- `_materialize_decorator:2419` existing inference/validation/dependency setup is reference; `_compile_decorators:5940` provides common component/lifespan/cleanup/dependency/sync plumbing. Ordinary predicates keep original core. Generated predicates share one recursively undecorated snapshot per eligible occurrence; skip snapshot if none.
- `_undecorated_component_view` (`components.py:432`) copies whole current graph today, risking quadratic work across unrelated roots. Restrict to relevant parent/dependency context while preserving ordinary parent/argument/owner facts and immutable snapshot semantics; no per-template copies.
- Combine ordinary/generated activation sort `(position,layer_index,-order,-source_order)` (ordinary source order zero). Earlier declarations/sources outermost at equal position; preserve layer precedence and independent additive templates.
- `_RegistrationStep`/`_CompiledDecorator` (`container.py:2550+`) own sequential sync/async activation/finalizers. Preserve inherited lifespan/cleanup and failures; generated cycles keep circular-dependency plus provenance.
- Publish usable agreed register/patch/remove methods on ComponentBuilder and builders; export RegistrationInfo/DecoratorTemplate. M04 private wrappers can be retained for tests but public behavior must work. Update M04 test asserting absent public methods. No public explanatory docs yet.
- Mandatory boundary test: actual group/DerivedServices generated decorator alters descendant-based Use/Expose matching; one-shot recheck catches changed/new-invalid selection with cause/provenance, without factory replay/fixed point. Synthetic M04 tests alone are insufficient.

Verification targets: new end-to-end portable sources/handlers using either/both/neither resource family; exact same-class/named sources,order permutations,opt-outs,nonmembers,decorator-only descendants,factory/instance/pattern/generic targets,identity-distinct vars,sync/async cleanup and callback no-replay. Existing container,complex dependencies,ownership,boundaries,expansion,feasibility,groups/targets suites. Ruff/ty/format/diff, relevant Python3.11 portability.

Coordinator read ordinary decorator pipeline and ordering. Search performed no edits/tests. Full boundary/overlay/edit acceptance remains M06; minimum visibility consistency is required now.
